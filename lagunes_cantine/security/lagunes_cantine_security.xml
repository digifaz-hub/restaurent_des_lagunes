<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="1">

        <record id="rule_lagunes_plat_multi_company" model="ir.rule">
            <field name="name">Plats - Multi Company</field>
            <field name="model_id" ref="model_lagunes_plat"/>
            <field name="domain_force">['|', ('company_id', '=', False), ('company_id', 'in', company_ids)]</field>
            <field name="groups" eval="[(4, ref('base.group_user'))]"/>
        </record>

        <record id="rule_lagunes_commande_multi_company" model="ir.rule">
            <field name="name">Commandes - Multi Company</field>
            <field name="model_id" ref="model_lagunes_commande"/>
            <field name="domain_force">['|', ('company_id', '=', False), ('company_id', 'in', company_ids)]</field>
            <field name="groups" eval="[(4, ref('base.group_user'))]"/>
        </record>

        <record id="rule_lagunes_week_menu_multi_company" model="ir.rule">
            <field name="name">Menus Hebdo - Multi Company</field>
            <field name="model_id" ref="model_lagunes_week_menu"/>
            <field name="domain_force">['|', ('company_id', '=', False), ('company_id', 'in', company_ids)]</field>
            <field name="groups" eval="[(4, ref('base.group_user'))]"/>
        </record>

        <record id="rule_lagunes_employe_multi_company" model="ir.rule">
            <field name="name">Employés - Multi Company</field>
            <field name="model_id" ref="model_lagunes_employe"/>
            <field name="domain_force">['|', ('company_id', '=', False), ('company_id', 'in', company_ids)]</field>
            <field name="groups" eval="[(4, ref('base.group_user'))]"/>
        </record>

    </data>
</odoo>
